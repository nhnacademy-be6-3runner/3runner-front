
server:
  port: 3000
spring:
  application:
    name: 3runner-front
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

  profiles:
    active: prod
