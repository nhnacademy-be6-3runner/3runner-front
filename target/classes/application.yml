spring:
  datasource:
    url: jdbc:mysql://133.186.241.167:3306/be6_3runner_dev
    username: be6_3runner
    password: tHos2g5st4@XH7mT
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    elasticsearch:
      repositories:
        enabled: true 
      url: localhost:9200
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true

    properties:
      hibernate:
      format_sql: true
      use_sql_comments: true
      highlight_sql: true
      auto_quote_keyword: true

  profiles:
    active: dev

  lifecycle:
    timeout-per-shutdown-phase: 30s

  application:
    name: 3runner-bookstore

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://admin:1234@eureka:8761/eureka
  instance:
    prefer-ip-address: true

management:
  endpoint:
    pause:
      enabled: true
    resume:
      enabled: true
    restart:
      enabled: true


server:
  shutdown: graceful

logging:
  level:
    org.hibernate.orm.jdbc.bind: trace
    org.springframework.transaction.interceptor: trace
